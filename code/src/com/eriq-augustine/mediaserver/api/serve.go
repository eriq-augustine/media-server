package api;

import (
   "com/eriq-augustine/mediaserver/log"
);

// TODO(eriq): Redirect to cache or raw.
// TODO(eriq): Do we need this?
func servePath(path string) {
   // TODO(eriq)
   log.Debug("Serving path: " + path);
}
